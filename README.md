# cryptoverse

## Run

1) Install deps:

```bash
npm ci
```

2) (Optional) Enable AI:

- Copy `.env.example` to `.env`
- Set `GEMINI_API_KEY`

3) Start:

```bash
npm start
```

Open `http://localhost:4000`.
